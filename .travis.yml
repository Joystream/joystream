language: rust
rust:
  - 1.32.0
cache: 
  - rust
  - cargo
jobs:
  include:
    - stage: build
    - provider: script
      script: bash setup.sh
      script: bash build.sh
#    - stage: deploy
#      script: ./deploy