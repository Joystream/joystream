language: rust
rust:
  - 1.32.0
cache: 
  - rust
  - cargo
jobs:
  include:
    - stage: setup
    - provider: script
      script: bash setup.sh
    - stage: build
      script: bash build.sh
#   - stage: deploy
      