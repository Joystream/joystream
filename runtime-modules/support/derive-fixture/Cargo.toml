[package]
name = "derive-fixture"
version = "0.1.0"
authors = ['Joystream contributors']
edition = "2018"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { workspace = true, features = ["default", "proc-macro"] }
quote = { workspace = true }
syn = { workspace = true }

[features]
default = ["std"]
std = []
