{
  "name": "storage-node",
  "version": "1.0.0",
  "description": "Joystream Storage Node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm-run-all --parallel server",
    "build": "npm-run-all --parallel server",
    "server": "nodemon --watch api.yaml --watch server.js --watch api/ --watch lib/ --verbose --exec node server.js --ext jsx,js"
  },
  "keywords": [
    "joystream",
    "storage",
    "node"
  ],
  "author": "Joystream",
  "devDependencies": {
    "express": "^4.16.3",
    "nodemon": "^1.18.3",
    "npm-run-all": "^4.1.3"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express-openapi": "^0.36.0",
    "js-yaml": "^0.3.7",
    "cors": "^0.1.1"
  }
}
