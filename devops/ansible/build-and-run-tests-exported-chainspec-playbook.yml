- hosts: 127.0.0.1
  user: root
  become: yes
  become_method: sudo

  tasks:

    - name: install dependencies
      include_role:
        name: install_dependencies

    - name: run tests
      include_role:
        name: run_tests_exported_chainspec
