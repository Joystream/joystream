{
  "extends": "../../tsconfig.json",

  "compilerOptions": {
    "module": "esnext",
    "rootDirs": ["src", "stories"],
    "baseUrl": "src",
    "jsx": "preserve",
    "declaration": true,
    "declarationDir": "dist/types"
  },

  "exclude": ["node_modules", "dist", "stories"]
}
